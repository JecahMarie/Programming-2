#include <stdio.h>
#include <conio.h>

int main(void)
{
    int Name;
    printf("Enter your Name: ");
    scanf("%d",&Name\n);

    int Age;
    printf("Enter your Age: ");
    scanf("%d", &Age);


    if(Age is <17)
    {
        printf("You are not eligible for voting");
            }
    else if(Age is >=18)
    {
        printf("you are eligible for voting");
            }
    else
        {
        printf("Invalid Age, Please try again later");
            }

    return 0;
}
